// Nodejs trabaja con módulos
const math = require('./math.js');

console.log(math)
// el resultado es un objeto con múltiples propiedades. cada propiedad es una función

/*
console.log(math.add(1,5));
console.log(math.substract(1,5));
console.log(math.multiply(1,5));
console.log(math.divide(1,0));
*/

//console.log(hello);